//docs/data-model.md
//今後実装予定のものをメモする

◯本番運用する場合は、ドメイン取得後に、.env.prodのMAIL_FROM=を"ARAO U-12 <取得したドメイン>"に変更し、
Vercelの環境変数（MAIL_FROM=）も変更し、Resendにドメイン登録をすること。
◯全ページ、ヘッダーとの距離、フッターとの距離、フォント、文字の大きさなどを統一する。
◯仮に現在のスパム対策をすり抜けるボットが出てきたら、自作スパム対策をやめてCloudflare Turnstileを導入する。
◯入会届のPDFは別タブで出るようにする
◯体験/見学管理一覧ページの15件ごとの「もっと見るボタン」の実装
◯お問い合わせ一覧の15件ごとの「もっと見るボタン」の実装
◯管理用練習スケジュール変更一覧の15件ごとの「もっと見るボタン」の実装
◯公開用練習スケジュール変更一覧の15件ごとの「もっと見るボタン」の実装
◯管理トップ（ダッシュボード）の５件ごとの「もっと見るボタン」の実装
◯model noticeの
eventDate DateTime?     @db.Date //（MVPv1.5にてカレンダー入力仕様。それまでは手入力）
を使用する実装にする
◯トップページに練習時間変更のお知らせが更新されましたの表示を出す（仮）
◯練習スケ新規作成で「時間変更」や「場所変更」、「時間場所変更」など内容をテンプレ化し選択できる機能があると、管理者側の手間が省ける
◯本番環境にseedでページコンテントのページキーとブロックキーの型を流し込む
◯画像保存の際がURL記入になっているので、画像選択からの保存の仕様にする
◯練習場所をGoogleと連携させる
◯チーム公式ラインを作成してHPで友達追加できるようにする（問い合わせしたのに回答が届かないなど不具合があった場合に公式ラインから連絡できるようにする）
◯DB上の画像データを削除する関数はあるが（deletePageContentImage）、supabaseストレージには残る仕様になってるので、supabaseストレージからも画像を削除できる仕様にする（imageUrl から Storage path を取り出す処理を追加すればできます）
◯ライン通知内に管理画面URLを入れる際に使うものは、完成後ドメイン取得時に変更すること
現在：APP_BASE_URL=https://arao-u12-hp.vercel.app
◯体験/見学申し込みおよび問い合わせ管理者通知をラインにおくる際の設定で、完成後、管理者名義で新しくチーム公式アカウントを作成して、そのアカウントで発行したものと取り替える。IDは、完成後に通知が来て欲しいグループや
現在：LINE_CHANNEL_ACCESS_TOKEN=（開発用アカウントのトークン）（ブラウザでMessaging APIをでアカウントを作成した後に、確認できる）
　　　LINE_ADMIN_GROUP_ID=（このLINEIDの人（あなた）に向けてメッセージを送る：LINE Developers（開発者画面）だけで確認できる Uから始まる33文字の英数字の文字列（ユーザーID））
◯問い合わせと体験申し込みの通知について：将来絶対に通知を届けたいのであれば「通知キュー」や「再送処理」が必要。現在は、送信時間を短くするための並列化のみ行なっている

◯以下の流れをコードを復習しながら理解する
LINEアプリに通知が届く
↓
LINE通知URLを押す
↓
/admin/liff が開く（エンドポイントURL）
↓
LIFF初期化
↓
必要ならLINEログイン
↓
/admin/contact/xxx へ遷移（元のURLに戻る）
↓
Supabase未ログインなら /admin/login
↓
ログイン後に管理画面

◯スムーズスクロールの部分をコード理解する

◯責務分離など完成後にPrettierを一度かける（コードを整える）

◯pagecontentsのアクション関数ファイルを再度見直す

◯インスタグラムのアカウントを作成し以下のURLを差し替える
export const INSTAGRAM_URL =
  "https://www.instagram.com/本物のアカウント名/";
※constants/publicNavigation.tsにある

◯ラインのURLを押してliffで管理者ページに遷移する際に、トップ（一番上）ではなく途中から始まるのをトップから始まるように治すこと

◯PCで管理者ページ内でヘッダーのロゴとドロワーを押すと遷移先でカクカク動く（スマホだとならない。公開ページだとならない。ローカルだとならない（本番のみなる））。サイト内文章設定ページ、パスワード変更ページ,メール通知設定だと2回上下する。

◯app/admin/(dashboard)/monthly-practice-plans/page.tsx をこうするか消す。

import { redirect } from "next/navigation";

export default function AdminMonthlyPracticePlansPage() {
  redirect("/admin/top-settings");
}